const { response } = require("express");
const { PromiseProvider } = require("mongoose");
const axios = require("axios");
class APISeach {
  async getThoiTiet(req, res) {}
}
module.exports = new APISeach();
